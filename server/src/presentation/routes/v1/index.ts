import { Router } from "express";

const v1Router = Router();

// Register v1 route modules here as the app grows:
// import authRoutes from "./auth.routes";
// v1Router.use("/auth", authRoutes);

export default v1Router;
